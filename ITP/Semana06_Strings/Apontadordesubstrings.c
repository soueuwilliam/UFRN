#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(){
    char textoA[41] = {0};
    char textoB[41] = {0};
    int i,j;
    int contador;

    fgets(textoA,40,stdin);
    fgets(textoB,40,stdin);

    
     printf("Repetição: %d",contador);
     printf("Posições:");

    return 0;
}