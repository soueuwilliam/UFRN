#include <stdio.h>

int main(){
    //declarando variaveis
    int decimal,binario = 0;
    int resto ;
    //recebendo entrada
    scanf("%d", &decimal);
    //calculo de binario
    while (decimal != 0 )
    {
     
    }
    printf("%d",binario);
    return 0;
}